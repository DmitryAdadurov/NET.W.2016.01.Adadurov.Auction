@model IEnumerable<UI.Mvc.Models.LotModel>

@foreach (var item in Model)
{
    <tr>
        <td>@Html.ActionLink(item.Title, "Details", new { id = item.Id })</td>
        <td>@item.CreationDate.ToLongDateString()</td>
        <td>@item.EndDate.ToLongDateString()</td>
    </tr>
}
